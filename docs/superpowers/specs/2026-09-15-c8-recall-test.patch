diff --git a/src/vault/facts.test.ts b/src/vault/facts.test.ts
--- a/src/vault/facts.test.ts
+++ b/src/vault/facts.test.ts
@@ -96,3 +96,4 @@
-  expect(getKnowledgeForMessage('Alex')).toContain('"validity":"outside recorded validity"');
-  expect(getKnowledgeForMessage('Alex')).toContain('"binding_eligible":false');
+  expect(getKnowledgeForMessage('Alex')).not.toContain('preferred_editor: Old');
+  expect(getKnowledgeForMessage('Alex')).not.toContain('old@example.com');
+  expect(getKnowledgeForMessage('Alex')).toContain('preferred_editor: Current');
 });
