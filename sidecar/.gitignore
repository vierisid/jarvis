# Build output
dist/
/jarvis
/jarvis.exe
/jarvis-test
/jarvis-test.exe
/sidecar
# `go build .` with no -o names its output after the directory. Deliberately no
# bare `/installer` entry: that is a TRACKED source directory, and ignoring it
# would hide every new file added to the installer package from git status.
/sidecar.exe
/installer.exe

# npm platform package binaries (built by make npm-pack)
npm/darwin-arm64/bin/
npm/darwin-x64/bin/
npm/linux-x64/bin/
npm/linux-arm64/bin/
npm/win32-x64/bin/
*.tgz

# Tester binaries (built by make test)
tester/bin/tester_*

# macOS Vision OCR helper (built by `make build-ocr-helper` on a Mac)
helpers/ocr-helper

# Installer build output (make build-installer / manifest-installer)
jarvis-setup
jarvis-setup.exe
installer/rsrc_windows_amd64.syso

# Generated resource scripts (version substituted from VERSION)
.jarvis.rc.gen
installer/.installer.rc.gen
